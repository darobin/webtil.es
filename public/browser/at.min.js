var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __commonJS = (cb, mod) => function __require() {
  return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));

// node_modules/@nanostores/lit/lib/StoreController.js
var require_StoreController = __commonJS({
  "node_modules/@nanostores/lit/lib/StoreController.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.StoreController = void 0;
    var StoreController2 = class {
      constructor(host, atom2) {
        this.host = host;
        this.atom = atom2;
        host.addController(this);
      }
      // Subscribe to the atom when the host connects
      hostConnected() {
        this.unsubscribe = this.atom.subscribe(() => {
          this.host.requestUpdate();
        });
      }
      // Unsubscribe from the atom when the host disconnects
      hostDisconnected() {
        var _a;
        (_a = this.unsubscribe) === null || _a === void 0 ? void 0 : _a.call(this);
      }
      /**
       * The current value of the atom.
       * @readonly
       */
      get value() {
        return this.atom.get();
      }
    };
    exports.StoreController = StoreController2;
  }
});

// node_modules/@nanostores/lit/lib/MultiStoreController.js
var require_MultiStoreController = __commonJS({
  "node_modules/@nanostores/lit/lib/MultiStoreController.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.MultiStoreController = void 0;
    var MultiStoreController = class {
      constructor(host, atoms) {
        this.host = host;
        this.atoms = atoms;
        host.addController(this);
      }
      // Subscribe to the atom when the host connects
      hostConnected() {
        this.unsubscribes = this.atoms.map((atom2) => atom2.subscribe(() => this.host.requestUpdate()));
      }
      // Unsubscribe from the atom when the host disconnects
      hostDisconnected() {
        var _a;
        (_a = this.unsubscribes) === null || _a === void 0 ? void 0 : _a.forEach((unsubscribe) => unsubscribe());
      }
      /**
       * The current values of the atoms.
       * @readonly
       */
      get values() {
        return this.atoms.map((atom2) => atom2.get());
      }
    };
    exports.MultiStoreController = MultiStoreController;
  }
});

// node_modules/@nanostores/lit/lib/useStores.js
var require_useStores = __commonJS({
  "node_modules/@nanostores/lit/lib/useStores.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.useStores = void 0;
    var MultiStoreController_1 = require_MultiStoreController();
    function useStores(...atoms) {
      return (constructor) => {
        return class extends constructor {
          constructor(...args) {
            super(...args);
            new MultiStoreController_1.MultiStoreController(this, atoms);
          }
        };
      };
    }
    exports.useStores = useStores;
  }
});

// node_modules/@nanostores/lit/lib/withStores.js
var require_withStores = __commonJS({
  "node_modules/@nanostores/lit/lib/withStores.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.withStores = void 0;
    var MultiStoreController_1 = require_MultiStoreController();
    var withStores = (LitElementClass, atoms) => {
      return class LitElementWithStores extends LitElementClass {
        constructor(...args) {
          super(...args);
          new MultiStoreController_1.MultiStoreController(this, atoms);
        }
      };
    };
    exports.withStores = withStores;
  }
});

// node_modules/@nanostores/lit/lib/index.js
var require_lib = __commonJS({
  "node_modules/@nanostores/lit/lib/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.withStores = exports.useStores = exports.MultiStoreController = exports.StoreController = void 0;
    var StoreController_1 = require_StoreController();
    Object.defineProperty(exports, "StoreController", { enumerable: true, get: function() {
      return StoreController_1.StoreController;
    } });
    var MultiStoreController_1 = require_MultiStoreController();
    Object.defineProperty(exports, "MultiStoreController", { enumerable: true, get: function() {
      return MultiStoreController_1.MultiStoreController;
    } });
    var useStores_1 = require_useStores();
    Object.defineProperty(exports, "useStores", { enumerable: true, get: function() {
      return useStores_1.useStores;
    } });
    var withStores_1 = require_withStores();
    Object.defineProperty(exports, "withStores", { enumerable: true, get: function() {
      return withStores_1.withStores;
    } });
  }
});

// node_modules/@dasl/tiles/lib/el.js
function el(n4, attrs, kids, p3) {
  const e4 = document.createElement(n4);
  Object.entries(attrs || {}).forEach(([k2, v2]) => {
    if (v2 == null) return;
    if (k2 === "style") {
      Object.entries(v2).forEach(([prop, value]) => {
        const snake = prop.split("-").map((part, idx) => idx ? part.charAt(0).toUpperCase() + part.slice(1) : part).join("");
        e4.style[snake] = value;
      });
      return;
    }
    e4.setAttribute(k2, v2);
  });
  (kids || []).forEach((n5) => {
    if (typeof n5 === "string") e4.append(txt(n5));
    else e4.append(n5);
  });
  if (p3) p3.append(e4);
  return e4;
}
function txt(str) {
  return document.createTextNode(str);
}

// node_modules/@dasl/tiles/loader.js
var TILES_PFX = "tiles-";
var SHUTTLE_PFX = "tiles-shuttle-";
var SND_SHUTTLE_LOAD = `${SHUTTLE_PFX}load`;
var RCV_SHUTTLE_READY = `${SHUTTLE_PFX}ready`;
var SND_SET_TITLE = `${SHUTTLE_PFX}set-title`;
var SND_SET_ICON = `${SHUTTLE_PFX}set-icon`;
var WORKER_PFX = "tiles-worker-";
var SND_WORKER_LOAD = `${WORKER_PFX}load`;
var RCV_WORKER_READY = `${WORKER_PFX}ready`;
var RCV_WORKER_REQUEST = `${WORKER_PFX}request`;
var SND_WORKER_RESPONSE = `${WORKER_PFX}response`;
var TILES_WARNING = `${TILES_PFX}warn`;
var TILES_ERROR = `${TILES_PFX}error`;
var TileMothership = class {
  #loaders = [];
  #conf = {};
  #id2shuttle = /* @__PURE__ */ new Map();
  #id2tile = /* @__PURE__ */ new Map();
  constructor(conf) {
    this.#conf = conf;
  }
  init() {
    window.addEventListener("message", async (ev) => {
      const { action } = ev.data || {};
      if (action === TILES_WARNING) {
        const { msg, id } = ev.data;
        console.warn(`[W:${id}]`, ...msg);
      }
      if (action === TILES_ERROR) {
        const { msg, id } = ev.data;
        console.error(`[S:${id}]`, ...msg);
      } else if (action === RCV_SHUTTLE_READY) {
        const { id } = ev.data;
        console.info(`[W:${id}] shuttle ready!`);
        this.sendToShuttle(id, SND_WORKER_LOAD, { id });
      } else if (action === RCV_WORKER_READY) {
        const { id } = ev.data;
        console.info(`[W:${id}] worker ready!`);
        const tile = this.#id2tile.get(id);
        if (!tile) throw new Error(`No tile shuttle with ID ${id}`);
        tile.dispatchEvent(new Event("load"));
      } else if (action === RCV_WORKER_REQUEST) {
        const { type, id, payload } = ev.data;
        console.info(`[W:${id}] worker request`, type, id, payload);
        if (type === "resolve-path") {
          const { path, requestId } = payload;
          const tile = this.#id2tile.get(id);
          if (!tile) throw new Error(`No tile shuttle with ID ${id}`);
          const { status, headers, body } = await tile.resolvePath(path);
          console.info(`[W:${id}] sending back`, status, headers, body);
          this.sendToShuttle(id, SND_WORKER_RESPONSE, { requestId, response: { status, headers, body } });
        }
      }
    });
  }
  sendToShuttle(id, action, payload) {
    const ifr = this.#id2shuttle.get(id);
    if (!ifr) return console.error(`No shuttle for ID ${id}`);
    ifr.contentWindow.postMessage({ id, action, payload }, "*");
  }
  registerShuttleFrame(ifr, tile) {
    const id = crypto.randomUUID();
    this.#id2shuttle.set(id, ifr);
    this.#id2tile.set(id, tile);
    return id;
  }
  startShuttle(id) {
    this.sendToShuttle(id, SND_SHUTTLE_LOAD, { id });
  }
  // Adds a loader that will handle matching requests to load a tile.
  // - `loader` is an object that knows how to load a tile for a specific scheme
  //    (and types)
  addLoader(loader) {
    this.#loaders.push(loader);
  }
  // Remove using same reference.
  removeLoader(loader) {
    this.#loaders = this.#loaders.filter((ldr) => ldr !== loader);
  }
  getLoadSource() {
    return `https://${this.#conf?.loadDomain || "load.webtil.es"}/.well-known/web-tiles/`;
  }
  // Load a tile.
  async loadTile(url) {
    let tile = false;
    for (const ldr of this.#loaders) {
      tile = await ldr.load(url, this);
      if (tile) break;
    }
    return tile;
  }
};
var Tile = class extends EventTarget {
  #mothership;
  #url;
  #manifest;
  #pathLoader;
  #shuttleId;
  constructor(mothership, url, manifest, pathLoader) {
    super();
    this.#mothership = mothership;
    this.#url = url;
    this.#manifest = manifest;
    this.#pathLoader = pathLoader;
    this.addEventListener("load", () => {
      if (this.#manifest?.name) {
        const title = this.#manifest?.name;
        this.#mothership.sendToShuttle(this.#shuttleId, SND_SET_TITLE, { title });
        const ev = new Event("title-change");
        ev.title = title;
        this.dispatchEvent(ev);
      }
      const icon = this.#manifest?.icons?.[0]?.src;
      if (icon) {
        this.#mothership.sendToShuttle(this.#shuttleId, SND_SET_ICON, { path: icon });
      }
    });
  }
  get url() {
    return this.#url;
  }
  get manifest() {
    return this.#manifest;
  }
  async resolvePath(path) {
    const u3 = new URL(`fake:${path}`);
    return this.#pathLoader.resolvePath(u3.pathname);
  }
  async renderCard(options) {
    const card = el("div", { style: {
      border: "1px solid lightgrey",
      "border-radius": "3px",
      cursor: "pointer"
    } });
    card.addEventListener("click", async () => {
      const tileRenderer = await this.renderContent(
        options?.contentHeight || this.#manifest?.sizing?.height || Math.max(card.offsetHeight, 300)
      );
      card.replaceWith(tileRenderer);
    });
    if (this.#manifest?.screenshots?.[0]?.src) {
      const res = await this.resolvePath(this.#manifest.screenshots[0].src);
      if (res.ok) {
        const blob = new Blob([res.body], { type: res.headers?.["content-type"] });
        const url = URL.createObjectURL(blob);
        el("div", { style: {
          "background-image": `url(${url})`,
          "background-size": "cover",
          "background-position": "50%",
          "aspect-ratio": "16/9"
        } }, [], card);
      }
    }
    const title = el("div", { style: {
      padding: "0.5rem 1rem",
      display: "flex",
      "align-items": "center"
    } }, [], card);
    if (this.#manifest?.icons?.[0]?.src) {
      const res = await this.resolvePath(this.#manifest.icons[0].src);
      if (res.ok) {
        const blob = new Blob([res.body], { type: res.headers?.["content-type"] });
        const url = URL.createObjectURL(blob);
        el("img", { src: url, width: "48", height: "48", alt: "icon", style: { "padding-right": "0.5rem" } }, [], title);
      }
    }
    el("span", { style: { "font-weight": "bold" } }, [this.#manifest.name || "Untitled Tile"], title);
    if (this.#manifest.description) {
      el("p", { style: { "margin": "0.5rem 1rem 1rem 1rem" } }, [this.#manifest.description], card);
    }
    return card;
  }
  getLoadSource() {
    return this.#mothership.getLoadSource();
  }
  attachIframe(ifr) {
    this.#shuttleId = this.#mothership.registerShuttleFrame(ifr, this);
    ifr.addEventListener("load", () => this.#mothership.startShuttle(this.#shuttleId));
  }
  renderContent(height = 300) {
    const ifr = el("iframe", {
      src: this.#mothership.getLoadSource(),
      style: {
        display: "block",
        width: "100%",
        height: `${height}px`,
        border: "none"
      }
    });
    this.attachIframe(ifr);
    return ifr;
  }
};

// node_modules/@dasl/tiles/lib/masl.js
var NOT_FOUND = { ok: false, status: 404, statusText: "Not found" };
var maslHeaders = [
  "content-disposition",
  "content-encoding",
  "content-language",
  "content-security-policy",
  "content-type",
  "link",
  "permissions-policy",
  "referrer-policy",
  "service-worker-allowed",
  "sourcemap",
  "speculation-rules",
  "supports-loading-mode",
  "x-content-type-options"
];
function maslResponse(masl, body) {
  if (!body) return NOT_FOUND;
  const headers = {};
  maslHeaders.forEach((k2) => {
    if (typeof masl[k2] !== "undefined") headers[k2] = masl[k2];
  });
  if (typeof body === "string") body = new TextEncoder().encode(body);
  return { ok: true, status: 200, statusText: "Ok", headers, body };
}

// node_modules/@dasl/tiles/loader/at.js
async function did2pds(did) {
  if (!/^did:plc:/.test(did)) return false;
  const res = await fetch(`https://plc.directory/${did}`);
  if (!res.ok) return false;
  const doc = await res.json();
  const pds = doc.service?.find((s5) => s5.id === "#atproto_pds")?.serviceEndpoint;
  return pds;
}
async function fetchFromPDS(did, query, params) {
  const pds = await did2pds(did);
  if (!pds) return;
  const url = new URL(pds);
  url.pathname = `/xrpc/${query}`;
  const prm = url.searchParams;
  Object.entries(params).forEach(([k2, v2]) => prm.set(k2, v2));
  return await fetch(url.toString());
}
async function getRecord(repo, collection, rkey) {
  const res = await fetchFromPDS(repo, "com.atproto.repo.getRecord", { repo, collection, rkey });
  if (!res.ok) return false;
  return await res.json();
}
async function getBlob(did, cid) {
  const res = await fetchFromPDS(did, "com.atproto.sync.getBlob", { did, cid });
  if (!res.ok) return false;
  return await res.arrayBuffer();
}
var ATTileLoader = class {
  async load(url, mothership) {
    if (!/^at:\/\//.test(url)) return false;
    const [repo, collection, rkey] = url.replace(/^at:\/\//, "").split("/");
    if (collection !== "ing.dasl.masl") return;
    const res = await getRecord(repo, collection, rkey);
    if (!res) return false;
    const manifest = res.value?.tile;
    if (!manifest) return false;
    const loader = new ATPathLoader(repo, manifest);
    return new Tile(mothership, url, manifest, loader);
  }
};
var ATPathLoader = class {
  #did;
  #manifest;
  constructor(did, manifest) {
    this.#did = did;
    this.#manifest = manifest;
  }
  async resolvePath(path) {
    const entry = this.#manifest?.resources?.[path];
    if (!entry?.src) return NOT_FOUND;
    const cid = entry.src.$link || entry.src.ref.$link;
    const data = await getBlob(this.#did, cid);
    if (!data) return NOT_FOUND;
    return maslResponse(entry, data);
  }
};

// node_modules/nanostores/clean-stores/index.js
var clean = Symbol("clean");

// node_modules/nanostores/atom/index.js
var listenerQueue = [];
var lqIndex = 0;
var QUEUE_ITEMS_PER_LISTENER = 4;
var epoch = 0;
var atom = /* @__NO_SIDE_EFFECTS__ */ (initialValue) => {
  let listeners = [];
  let $atom = {
    get() {
      if (!$atom.lc) {
        $atom.listen(() => {
        })();
      }
      return $atom.value;
    },
    lc: 0,
    listen(listener) {
      $atom.lc = listeners.push(listener);
      return () => {
        for (let i6 = lqIndex + QUEUE_ITEMS_PER_LISTENER; i6 < listenerQueue.length; ) {
          if (listenerQueue[i6] === listener) {
            listenerQueue.splice(i6, QUEUE_ITEMS_PER_LISTENER);
          } else {
            i6 += QUEUE_ITEMS_PER_LISTENER;
          }
        }
        let index = listeners.indexOf(listener);
        if (~index) {
          listeners.splice(index, 1);
          if (!--$atom.lc) $atom.off();
        }
      };
    },
    notify(oldValue, changedKey) {
      epoch++;
      let runListenerQueue = !listenerQueue.length;
      for (let listener of listeners) {
        listenerQueue.push(listener, $atom.value, oldValue, changedKey);
      }
      if (runListenerQueue) {
        for (lqIndex = 0; lqIndex < listenerQueue.length; lqIndex += QUEUE_ITEMS_PER_LISTENER) {
          listenerQueue[lqIndex](
            listenerQueue[lqIndex + 1],
            listenerQueue[lqIndex + 2],
            listenerQueue[lqIndex + 3]
          );
        }
        listenerQueue.length = 0;
      }
    },
    /* It will be called on last listener unsubscribing.
       We will redefine it in onMount and onStop. */
    off() {
    },
    set(newValue) {
      let oldValue = $atom.value;
      if (oldValue !== newValue) {
        $atom.value = newValue;
        $atom.notify(oldValue);
      }
    },
    subscribe(listener) {
      let unbind = $atom.listen(listener);
      listener($atom.value);
      return unbind;
    },
    value: initialValue
  };
  if (true) {
    $atom[clean] = () => {
      listeners = [];
      $atom.lc = 0;
      $atom.off();
    };
  }
  return $atom;
};

// public/browser/stores.js
async function fetchFromBsky(query, params) {
  const url = new URL(`https://public.api.bsky.app/xrpc/${query}`);
  const prm = url.searchParams;
  Object.entries(params).forEach(([k2, v2]) => prm.set(k2, v2));
  return await fetch(url.toString());
}
async function resolveHandle(handle) {
  const res = await fetchFromBsky("com.atproto.identity.resolveHandle", { handle });
  if (!res.ok) return;
  const { did } = await res.json();
  return did;
}
async function getProfile(actor) {
  const res = await fetchFromBsky("app.bsky.actor.getProfile", { actor });
  if (!res.ok) return {};
  return await res.json();
}
async function didifyURL(url) {
  if (/^at:\/\/did:/.test(url)) return url;
  const [repo, collection, rkey] = url.replace(/^at:\/\//, "").split("/");
  const did = await resolveHandle(repo);
  console.warn(`DIDIFIED: at://${did}/${collection}/${rkey}`);
  return `at://${did}/${collection}/${rkey}`;
}
var loadDomain = /localhost|\.bast/.test(window.location.hostname) ? "load.webtiles.bast" : "load.webtil.es";
var at = new ATTileLoader();
var tl = new TileMothership({ loadDomain });
tl.init();
tl.addLoader(at);
var $url = atom();
async function navigate(url) {
  if (!url) return;
  $error.set(null);
  $url.set(url);
  const tile = await tl.loadTile(await didifyURL(url));
  console.warn(`Tile`, tile);
  $currentTile.set(tile);
}
function navigateFromHash() {
  const { hash } = window.location;
  const params = {};
  hash.replace(/^#/, "").split("&").filter(Boolean).forEach((prm) => {
    const [k2, v2] = prm.split(/=/, 2);
    params[k2] = typeof v2 === "undefined" ? true : v2;
  });
  navigate(params.url);
}
var $currentTile = atom();
var $error = atom();
function error(msg) {
  $error.set(msg);
}
var defaultTitle = "ATMOS";
var $title = atom(defaultTitle);
function setTitle(title) {
  $title.set(title || defaultTitle);
}
$currentTile.subscribe((tile) => {
  setTitle(tile?.manifest?.name);
});
var $profile = atom();
$currentTile.subscribe((tile) => {
  if (tile) {
    const [actor] = tile.url.replace(/^at:\/\//, "").split("/");
    getProfile(actor).then((p3) => $profile.set(p3));
  }
});

// node_modules/@lit/reactive-element/css-tag.js
var t = globalThis;
var e = t.ShadowRoot && (void 0 === t.ShadyCSS || t.ShadyCSS.nativeShadow) && "adoptedStyleSheets" in Document.prototype && "replace" in CSSStyleSheet.prototype;
var s = Symbol();
var o = /* @__PURE__ */ new WeakMap();
var n = class {
  constructor(t3, e4, o5) {
    if (this._$cssResult$ = true, o5 !== s) throw Error("CSSResult is not constructable. Use `unsafeCSS` or `css` instead.");
    this.cssText = t3, this.t = e4;
  }
  get styleSheet() {
    let t3 = this.o;
    const s5 = this.t;
    if (e && void 0 === t3) {
      const e4 = void 0 !== s5 && 1 === s5.length;
      e4 && (t3 = o.get(s5)), void 0 === t3 && ((this.o = t3 = new CSSStyleSheet()).replaceSync(this.cssText), e4 && o.set(s5, t3));
    }
    return t3;
  }
  toString() {
    return this.cssText;
  }
};
var r = (t3) => new n("string" == typeof t3 ? t3 : t3 + "", void 0, s);
var i = (t3, ...e4) => {
  const o5 = 1 === t3.length ? t3[0] : e4.reduce((e5, s5, o6) => e5 + ((t4) => {
    if (true === t4._$cssResult$) return t4.cssText;
    if ("number" == typeof t4) return t4;
    throw Error("Value passed to 'css' function must be a 'css' function result: " + t4 + ". Use 'unsafeCSS' to pass non-literal values, but take care to ensure page security.");
  })(s5) + t3[o6 + 1], t3[0]);
  return new n(o5, t3, s);
};
var S = (s5, o5) => {
  if (e) s5.adoptedStyleSheets = o5.map((t3) => t3 instanceof CSSStyleSheet ? t3 : t3.styleSheet);
  else for (const e4 of o5) {
    const o6 = document.createElement("style"), n4 = t.litNonce;
    void 0 !== n4 && o6.setAttribute("nonce", n4), o6.textContent = e4.cssText, s5.appendChild(o6);
  }
};
var c = e ? (t3) => t3 : (t3) => t3 instanceof CSSStyleSheet ? ((t4) => {
  let e4 = "";
  for (const s5 of t4.cssRules) e4 += s5.cssText;
  return r(e4);
})(t3) : t3;

// node_modules/@lit/reactive-element/reactive-element.js
var { is: i2, defineProperty: e2, getOwnPropertyDescriptor: h, getOwnPropertyNames: r2, getOwnPropertySymbols: o2, getPrototypeOf: n2 } = Object;
var a = globalThis;
var c2 = a.trustedTypes;
var l = c2 ? c2.emptyScript : "";
var p = a.reactiveElementPolyfillSupport;
var d = (t3, s5) => t3;
var u = { toAttribute(t3, s5) {
  switch (s5) {
    case Boolean:
      t3 = t3 ? l : null;
      break;
    case Object:
    case Array:
      t3 = null == t3 ? t3 : JSON.stringify(t3);
  }
  return t3;
}, fromAttribute(t3, s5) {
  let i6 = t3;
  switch (s5) {
    case Boolean:
      i6 = null !== t3;
      break;
    case Number:
      i6 = null === t3 ? null : Number(t3);
      break;
    case Object:
    case Array:
      try {
        i6 = JSON.parse(t3);
      } catch (t4) {
        i6 = null;
      }
  }
  return i6;
} };
var f = (t3, s5) => !i2(t3, s5);
var b = { attribute: true, type: String, converter: u, reflect: false, useDefault: false, hasChanged: f };
Symbol.metadata ??= Symbol("metadata"), a.litPropertyMetadata ??= /* @__PURE__ */ new WeakMap();
var y = class extends HTMLElement {
  static addInitializer(t3) {
    this._$Ei(), (this.l ??= []).push(t3);
  }
  static get observedAttributes() {
    return this.finalize(), this._$Eh && [...this._$Eh.keys()];
  }
  static createProperty(t3, s5 = b) {
    if (s5.state && (s5.attribute = false), this._$Ei(), this.prototype.hasOwnProperty(t3) && ((s5 = Object.create(s5)).wrapped = true), this.elementProperties.set(t3, s5), !s5.noAccessor) {
      const i6 = Symbol(), h4 = this.getPropertyDescriptor(t3, i6, s5);
      void 0 !== h4 && e2(this.prototype, t3, h4);
    }
  }
  static getPropertyDescriptor(t3, s5, i6) {
    const { get: e4, set: r5 } = h(this.prototype, t3) ?? { get() {
      return this[s5];
    }, set(t4) {
      this[s5] = t4;
    } };
    return { get: e4, set(s6) {
      const h4 = e4?.call(this);
      r5?.call(this, s6), this.requestUpdate(t3, h4, i6);
    }, configurable: true, enumerable: true };
  }
  static getPropertyOptions(t3) {
    return this.elementProperties.get(t3) ?? b;
  }
  static _$Ei() {
    if (this.hasOwnProperty(d("elementProperties"))) return;
    const t3 = n2(this);
    t3.finalize(), void 0 !== t3.l && (this.l = [...t3.l]), this.elementProperties = new Map(t3.elementProperties);
  }
  static finalize() {
    if (this.hasOwnProperty(d("finalized"))) return;
    if (this.finalized = true, this._$Ei(), this.hasOwnProperty(d("properties"))) {
      const t4 = this.properties, s5 = [...r2(t4), ...o2(t4)];
      for (const i6 of s5) this.createProperty(i6, t4[i6]);
    }
    const t3 = this[Symbol.metadata];
    if (null !== t3) {
      const s5 = litPropertyMetadata.get(t3);
      if (void 0 !== s5) for (const [t4, i6] of s5) this.elementProperties.set(t4, i6);
    }
    this._$Eh = /* @__PURE__ */ new Map();
    for (const [t4, s5] of this.elementProperties) {
      const i6 = this._$Eu(t4, s5);
      void 0 !== i6 && this._$Eh.set(i6, t4);
    }
    this.elementStyles = this.finalizeStyles(this.styles);
  }
  static finalizeStyles(s5) {
    const i6 = [];
    if (Array.isArray(s5)) {
      const e4 = new Set(s5.flat(1 / 0).reverse());
      for (const s6 of e4) i6.unshift(c(s6));
    } else void 0 !== s5 && i6.push(c(s5));
    return i6;
  }
  static _$Eu(t3, s5) {
    const i6 = s5.attribute;
    return false === i6 ? void 0 : "string" == typeof i6 ? i6 : "string" == typeof t3 ? t3.toLowerCase() : void 0;
  }
  constructor() {
    super(), this._$Ep = void 0, this.isUpdatePending = false, this.hasUpdated = false, this._$Em = null, this._$Ev();
  }
  _$Ev() {
    this._$ES = new Promise((t3) => this.enableUpdating = t3), this._$AL = /* @__PURE__ */ new Map(), this._$E_(), this.requestUpdate(), this.constructor.l?.forEach((t3) => t3(this));
  }
  addController(t3) {
    (this._$EO ??= /* @__PURE__ */ new Set()).add(t3), void 0 !== this.renderRoot && this.isConnected && t3.hostConnected?.();
  }
  removeController(t3) {
    this._$EO?.delete(t3);
  }
  _$E_() {
    const t3 = /* @__PURE__ */ new Map(), s5 = this.constructor.elementProperties;
    for (const i6 of s5.keys()) this.hasOwnProperty(i6) && (t3.set(i6, this[i6]), delete this[i6]);
    t3.size > 0 && (this._$Ep = t3);
  }
  createRenderRoot() {
    const t3 = this.shadowRoot ?? this.attachShadow(this.constructor.shadowRootOptions);
    return S(t3, this.constructor.elementStyles), t3;
  }
  connectedCallback() {
    this.renderRoot ??= this.createRenderRoot(), this.enableUpdating(true), this._$EO?.forEach((t3) => t3.hostConnected?.());
  }
  enableUpdating(t3) {
  }
  disconnectedCallback() {
    this._$EO?.forEach((t3) => t3.hostDisconnected?.());
  }
  attributeChangedCallback(t3, s5, i6) {
    this._$AK(t3, i6);
  }
  _$ET(t3, s5) {
    const i6 = this.constructor.elementProperties.get(t3), e4 = this.constructor._$Eu(t3, i6);
    if (void 0 !== e4 && true === i6.reflect) {
      const h4 = (void 0 !== i6.converter?.toAttribute ? i6.converter : u).toAttribute(s5, i6.type);
      this._$Em = t3, null == h4 ? this.removeAttribute(e4) : this.setAttribute(e4, h4), this._$Em = null;
    }
  }
  _$AK(t3, s5) {
    const i6 = this.constructor, e4 = i6._$Eh.get(t3);
    if (void 0 !== e4 && this._$Em !== e4) {
      const t4 = i6.getPropertyOptions(e4), h4 = "function" == typeof t4.converter ? { fromAttribute: t4.converter } : void 0 !== t4.converter?.fromAttribute ? t4.converter : u;
      this._$Em = e4;
      const r5 = h4.fromAttribute(s5, t4.type);
      this[e4] = r5 ?? this._$Ej?.get(e4) ?? r5, this._$Em = null;
    }
  }
  requestUpdate(t3, s5, i6, e4 = false, h4) {
    if (void 0 !== t3) {
      const r5 = this.constructor;
      if (false === e4 && (h4 = this[t3]), i6 ??= r5.getPropertyOptions(t3), !((i6.hasChanged ?? f)(h4, s5) || i6.useDefault && i6.reflect && h4 === this._$Ej?.get(t3) && !this.hasAttribute(r5._$Eu(t3, i6)))) return;
      this.C(t3, s5, i6);
    }
    false === this.isUpdatePending && (this._$ES = this._$EP());
  }
  C(t3, s5, { useDefault: i6, reflect: e4, wrapped: h4 }, r5) {
    i6 && !(this._$Ej ??= /* @__PURE__ */ new Map()).has(t3) && (this._$Ej.set(t3, r5 ?? s5 ?? this[t3]), true !== h4 || void 0 !== r5) || (this._$AL.has(t3) || (this.hasUpdated || i6 || (s5 = void 0), this._$AL.set(t3, s5)), true === e4 && this._$Em !== t3 && (this._$Eq ??= /* @__PURE__ */ new Set()).add(t3));
  }
  async _$EP() {
    this.isUpdatePending = true;
    try {
      await this._$ES;
    } catch (t4) {
      Promise.reject(t4);
    }
    const t3 = this.scheduleUpdate();
    return null != t3 && await t3, !this.isUpdatePending;
  }
  scheduleUpdate() {
    return this.performUpdate();
  }
  performUpdate() {
    if (!this.isUpdatePending) return;
    if (!this.hasUpdated) {
      if (this.renderRoot ??= this.createRenderRoot(), this._$Ep) {
        for (const [t5, s6] of this._$Ep) this[t5] = s6;
        this._$Ep = void 0;
      }
      const t4 = this.constructor.elementProperties;
      if (t4.size > 0) for (const [s6, i6] of t4) {
        const { wrapped: t5 } = i6, e4 = this[s6];
        true !== t5 || this._$AL.has(s6) || void 0 === e4 || this.C(s6, void 0, i6, e4);
      }
    }
    let t3 = false;
    const s5 = this._$AL;
    try {
      t3 = this.shouldUpdate(s5), t3 ? (this.willUpdate(s5), this._$EO?.forEach((t4) => t4.hostUpdate?.()), this.update(s5)) : this._$EM();
    } catch (s6) {
      throw t3 = false, this._$EM(), s6;
    }
    t3 && this._$AE(s5);
  }
  willUpdate(t3) {
  }
  _$AE(t3) {
    this._$EO?.forEach((t4) => t4.hostUpdated?.()), this.hasUpdated || (this.hasUpdated = true, this.firstUpdated(t3)), this.updated(t3);
  }
  _$EM() {
    this._$AL = /* @__PURE__ */ new Map(), this.isUpdatePending = false;
  }
  get updateComplete() {
    return this.getUpdateComplete();
  }
  getUpdateComplete() {
    return this._$ES;
  }
  shouldUpdate(t3) {
    return true;
  }
  update(t3) {
    this._$Eq &&= this._$Eq.forEach((t4) => this._$ET(t4, this[t4])), this._$EM();
  }
  updated(t3) {
  }
  firstUpdated(t3) {
  }
};
y.elementStyles = [], y.shadowRootOptions = { mode: "open" }, y[d("elementProperties")] = /* @__PURE__ */ new Map(), y[d("finalized")] = /* @__PURE__ */ new Map(), p?.({ ReactiveElement: y }), (a.reactiveElementVersions ??= []).push("2.1.2");

// node_modules/lit-html/lit-html.js
var t2 = globalThis;
var i3 = (t3) => t3;
var s2 = t2.trustedTypes;
var e3 = s2 ? s2.createPolicy("lit-html", { createHTML: (t3) => t3 }) : void 0;
var h2 = "$lit$";
var o3 = `lit$${Math.random().toFixed(9).slice(2)}$`;
var n3 = "?" + o3;
var r3 = `<${n3}>`;
var l2 = document;
var c3 = () => l2.createComment("");
var a2 = (t3) => null === t3 || "object" != typeof t3 && "function" != typeof t3;
var u2 = Array.isArray;
var d2 = (t3) => u2(t3) || "function" == typeof t3?.[Symbol.iterator];
var f2 = "[ 	\n\f\r]";
var v = /<(?:(!--|\/[^a-zA-Z])|(\/?[a-zA-Z][^>\s]*)|(\/?$))/g;
var _ = /-->/g;
var m = />/g;
var p2 = RegExp(`>|${f2}(?:([^\\s"'>=/]+)(${f2}*=${f2}*(?:[^ 	
\f\r"'\`<>=]|("|')|))|$)`, "g");
var g = /'/g;
var $ = /"/g;
var y2 = /^(?:script|style|textarea|title)$/i;
var x = (t3) => (i6, ...s5) => ({ _$litType$: t3, strings: i6, values: s5 });
var b2 = x(1);
var w = x(2);
var T = x(3);
var E = Symbol.for("lit-noChange");
var A = Symbol.for("lit-nothing");
var C = /* @__PURE__ */ new WeakMap();
var P = l2.createTreeWalker(l2, 129);
function V(t3, i6) {
  if (!u2(t3) || !t3.hasOwnProperty("raw")) throw Error("invalid template strings array");
  return void 0 !== e3 ? e3.createHTML(i6) : i6;
}
var N = (t3, i6) => {
  const s5 = t3.length - 1, e4 = [];
  let n4, l3 = 2 === i6 ? "<svg>" : 3 === i6 ? "<math>" : "", c4 = v;
  for (let i7 = 0; i7 < s5; i7++) {
    const s6 = t3[i7];
    let a3, u3, d3 = -1, f3 = 0;
    for (; f3 < s6.length && (c4.lastIndex = f3, u3 = c4.exec(s6), null !== u3); ) f3 = c4.lastIndex, c4 === v ? "!--" === u3[1] ? c4 = _ : void 0 !== u3[1] ? c4 = m : void 0 !== u3[2] ? (y2.test(u3[2]) && (n4 = RegExp("</" + u3[2], "g")), c4 = p2) : void 0 !== u3[3] && (c4 = p2) : c4 === p2 ? ">" === u3[0] ? (c4 = n4 ?? v, d3 = -1) : void 0 === u3[1] ? d3 = -2 : (d3 = c4.lastIndex - u3[2].length, a3 = u3[1], c4 = void 0 === u3[3] ? p2 : '"' === u3[3] ? $ : g) : c4 === $ || c4 === g ? c4 = p2 : c4 === _ || c4 === m ? c4 = v : (c4 = p2, n4 = void 0);
    const x2 = c4 === p2 && t3[i7 + 1].startsWith("/>") ? " " : "";
    l3 += c4 === v ? s6 + r3 : d3 >= 0 ? (e4.push(a3), s6.slice(0, d3) + h2 + s6.slice(d3) + o3 + x2) : s6 + o3 + (-2 === d3 ? i7 : x2);
  }
  return [V(t3, l3 + (t3[s5] || "<?>") + (2 === i6 ? "</svg>" : 3 === i6 ? "</math>" : "")), e4];
};
var S2 = class _S {
  constructor({ strings: t3, _$litType$: i6 }, e4) {
    let r5;
    this.parts = [];
    let l3 = 0, a3 = 0;
    const u3 = t3.length - 1, d3 = this.parts, [f3, v2] = N(t3, i6);
    if (this.el = _S.createElement(f3, e4), P.currentNode = this.el.content, 2 === i6 || 3 === i6) {
      const t4 = this.el.content.firstChild;
      t4.replaceWith(...t4.childNodes);
    }
    for (; null !== (r5 = P.nextNode()) && d3.length < u3; ) {
      if (1 === r5.nodeType) {
        if (r5.hasAttributes()) for (const t4 of r5.getAttributeNames()) if (t4.endsWith(h2)) {
          const i7 = v2[a3++], s5 = r5.getAttribute(t4).split(o3), e5 = /([.?@])?(.*)/.exec(i7);
          d3.push({ type: 1, index: l3, name: e5[2], strings: s5, ctor: "." === e5[1] ? I : "?" === e5[1] ? L : "@" === e5[1] ? z : H }), r5.removeAttribute(t4);
        } else t4.startsWith(o3) && (d3.push({ type: 6, index: l3 }), r5.removeAttribute(t4));
        if (y2.test(r5.tagName)) {
          const t4 = r5.textContent.split(o3), i7 = t4.length - 1;
          if (i7 > 0) {
            r5.textContent = s2 ? s2.emptyScript : "";
            for (let s5 = 0; s5 < i7; s5++) r5.append(t4[s5], c3()), P.nextNode(), d3.push({ type: 2, index: ++l3 });
            r5.append(t4[i7], c3());
          }
        }
      } else if (8 === r5.nodeType) if (r5.data === n3) d3.push({ type: 2, index: l3 });
      else {
        let t4 = -1;
        for (; -1 !== (t4 = r5.data.indexOf(o3, t4 + 1)); ) d3.push({ type: 7, index: l3 }), t4 += o3.length - 1;
      }
      l3++;
    }
  }
  static createElement(t3, i6) {
    const s5 = l2.createElement("template");
    return s5.innerHTML = t3, s5;
  }
};
function M(t3, i6, s5 = t3, e4) {
  if (i6 === E) return i6;
  let h4 = void 0 !== e4 ? s5._$Co?.[e4] : s5._$Cl;
  const o5 = a2(i6) ? void 0 : i6._$litDirective$;
  return h4?.constructor !== o5 && (h4?._$AO?.(false), void 0 === o5 ? h4 = void 0 : (h4 = new o5(t3), h4._$AT(t3, s5, e4)), void 0 !== e4 ? (s5._$Co ??= [])[e4] = h4 : s5._$Cl = h4), void 0 !== h4 && (i6 = M(t3, h4._$AS(t3, i6.values), h4, e4)), i6;
}
var R = class {
  constructor(t3, i6) {
    this._$AV = [], this._$AN = void 0, this._$AD = t3, this._$AM = i6;
  }
  get parentNode() {
    return this._$AM.parentNode;
  }
  get _$AU() {
    return this._$AM._$AU;
  }
  u(t3) {
    const { el: { content: i6 }, parts: s5 } = this._$AD, e4 = (t3?.creationScope ?? l2).importNode(i6, true);
    P.currentNode = e4;
    let h4 = P.nextNode(), o5 = 0, n4 = 0, r5 = s5[0];
    for (; void 0 !== r5; ) {
      if (o5 === r5.index) {
        let i7;
        2 === r5.type ? i7 = new k(h4, h4.nextSibling, this, t3) : 1 === r5.type ? i7 = new r5.ctor(h4, r5.name, r5.strings, this, t3) : 6 === r5.type && (i7 = new Z(h4, this, t3)), this._$AV.push(i7), r5 = s5[++n4];
      }
      o5 !== r5?.index && (h4 = P.nextNode(), o5++);
    }
    return P.currentNode = l2, e4;
  }
  p(t3) {
    let i6 = 0;
    for (const s5 of this._$AV) void 0 !== s5 && (void 0 !== s5.strings ? (s5._$AI(t3, s5, i6), i6 += s5.strings.length - 2) : s5._$AI(t3[i6])), i6++;
  }
};
var k = class _k {
  get _$AU() {
    return this._$AM?._$AU ?? this._$Cv;
  }
  constructor(t3, i6, s5, e4) {
    this.type = 2, this._$AH = A, this._$AN = void 0, this._$AA = t3, this._$AB = i6, this._$AM = s5, this.options = e4, this._$Cv = e4?.isConnected ?? true;
  }
  get parentNode() {
    let t3 = this._$AA.parentNode;
    const i6 = this._$AM;
    return void 0 !== i6 && 11 === t3?.nodeType && (t3 = i6.parentNode), t3;
  }
  get startNode() {
    return this._$AA;
  }
  get endNode() {
    return this._$AB;
  }
  _$AI(t3, i6 = this) {
    t3 = M(this, t3, i6), a2(t3) ? t3 === A || null == t3 || "" === t3 ? (this._$AH !== A && this._$AR(), this._$AH = A) : t3 !== this._$AH && t3 !== E && this._(t3) : void 0 !== t3._$litType$ ? this.$(t3) : void 0 !== t3.nodeType ? this.T(t3) : d2(t3) ? this.k(t3) : this._(t3);
  }
  O(t3) {
    return this._$AA.parentNode.insertBefore(t3, this._$AB);
  }
  T(t3) {
    this._$AH !== t3 && (this._$AR(), this._$AH = this.O(t3));
  }
  _(t3) {
    this._$AH !== A && a2(this._$AH) ? this._$AA.nextSibling.data = t3 : this.T(l2.createTextNode(t3)), this._$AH = t3;
  }
  $(t3) {
    const { values: i6, _$litType$: s5 } = t3, e4 = "number" == typeof s5 ? this._$AC(t3) : (void 0 === s5.el && (s5.el = S2.createElement(V(s5.h, s5.h[0]), this.options)), s5);
    if (this._$AH?._$AD === e4) this._$AH.p(i6);
    else {
      const t4 = new R(e4, this), s6 = t4.u(this.options);
      t4.p(i6), this.T(s6), this._$AH = t4;
    }
  }
  _$AC(t3) {
    let i6 = C.get(t3.strings);
    return void 0 === i6 && C.set(t3.strings, i6 = new S2(t3)), i6;
  }
  k(t3) {
    u2(this._$AH) || (this._$AH = [], this._$AR());
    const i6 = this._$AH;
    let s5, e4 = 0;
    for (const h4 of t3) e4 === i6.length ? i6.push(s5 = new _k(this.O(c3()), this.O(c3()), this, this.options)) : s5 = i6[e4], s5._$AI(h4), e4++;
    e4 < i6.length && (this._$AR(s5 && s5._$AB.nextSibling, e4), i6.length = e4);
  }
  _$AR(t3 = this._$AA.nextSibling, s5) {
    for (this._$AP?.(false, true, s5); t3 !== this._$AB; ) {
      const s6 = i3(t3).nextSibling;
      i3(t3).remove(), t3 = s6;
    }
  }
  setConnected(t3) {
    void 0 === this._$AM && (this._$Cv = t3, this._$AP?.(t3));
  }
};
var H = class {
  get tagName() {
    return this.element.tagName;
  }
  get _$AU() {
    return this._$AM._$AU;
  }
  constructor(t3, i6, s5, e4, h4) {
    this.type = 1, this._$AH = A, this._$AN = void 0, this.element = t3, this.name = i6, this._$AM = e4, this.options = h4, s5.length > 2 || "" !== s5[0] || "" !== s5[1] ? (this._$AH = Array(s5.length - 1).fill(new String()), this.strings = s5) : this._$AH = A;
  }
  _$AI(t3, i6 = this, s5, e4) {
    const h4 = this.strings;
    let o5 = false;
    if (void 0 === h4) t3 = M(this, t3, i6, 0), o5 = !a2(t3) || t3 !== this._$AH && t3 !== E, o5 && (this._$AH = t3);
    else {
      const e5 = t3;
      let n4, r5;
      for (t3 = h4[0], n4 = 0; n4 < h4.length - 1; n4++) r5 = M(this, e5[s5 + n4], i6, n4), r5 === E && (r5 = this._$AH[n4]), o5 ||= !a2(r5) || r5 !== this._$AH[n4], r5 === A ? t3 = A : t3 !== A && (t3 += (r5 ?? "") + h4[n4 + 1]), this._$AH[n4] = r5;
    }
    o5 && !e4 && this.j(t3);
  }
  j(t3) {
    t3 === A ? this.element.removeAttribute(this.name) : this.element.setAttribute(this.name, t3 ?? "");
  }
};
var I = class extends H {
  constructor() {
    super(...arguments), this.type = 3;
  }
  j(t3) {
    this.element[this.name] = t3 === A ? void 0 : t3;
  }
};
var L = class extends H {
  constructor() {
    super(...arguments), this.type = 4;
  }
  j(t3) {
    this.element.toggleAttribute(this.name, !!t3 && t3 !== A);
  }
};
var z = class extends H {
  constructor(t3, i6, s5, e4, h4) {
    super(t3, i6, s5, e4, h4), this.type = 5;
  }
  _$AI(t3, i6 = this) {
    if ((t3 = M(this, t3, i6, 0) ?? A) === E) return;
    const s5 = this._$AH, e4 = t3 === A && s5 !== A || t3.capture !== s5.capture || t3.once !== s5.once || t3.passive !== s5.passive, h4 = t3 !== A && (s5 === A || e4);
    e4 && this.element.removeEventListener(this.name, this, s5), h4 && this.element.addEventListener(this.name, this, t3), this._$AH = t3;
  }
  handleEvent(t3) {
    "function" == typeof this._$AH ? this._$AH.call(this.options?.host ?? this.element, t3) : this._$AH.handleEvent(t3);
  }
};
var Z = class {
  constructor(t3, i6, s5) {
    this.element = t3, this.type = 6, this._$AN = void 0, this._$AM = i6, this.options = s5;
  }
  get _$AU() {
    return this._$AM._$AU;
  }
  _$AI(t3) {
    M(this, t3);
  }
};
var B = t2.litHtmlPolyfillSupport;
B?.(S2, k), (t2.litHtmlVersions ??= []).push("3.3.2");
var D = (t3, i6, s5) => {
  const e4 = s5?.renderBefore ?? i6;
  let h4 = e4._$litPart$;
  if (void 0 === h4) {
    const t4 = s5?.renderBefore ?? null;
    e4._$litPart$ = h4 = new k(i6.insertBefore(c3(), t4), t4, void 0, s5 ?? {});
  }
  return h4._$AI(t3), h4;
};

// node_modules/lit-element/lit-element.js
var s3 = globalThis;
var i4 = class extends y {
  constructor() {
    super(...arguments), this.renderOptions = { host: this }, this._$Do = void 0;
  }
  createRenderRoot() {
    const t3 = super.createRenderRoot();
    return this.renderOptions.renderBefore ??= t3.firstChild, t3;
  }
  update(t3) {
    const r5 = this.render();
    this.hasUpdated || (this.renderOptions.isConnected = this.isConnected), super.update(t3), this._$Do = D(r5, this.renderRoot, this.renderOptions);
  }
  connectedCallback() {
    super.connectedCallback(), this._$Do?.setConnected(true);
  }
  disconnectedCallback() {
    super.disconnectedCallback(), this._$Do?.setConnected(false);
  }
  render() {
    return E;
  }
};
i4._$litElement$ = true, i4["finalized"] = true, s3.litElementHydrateSupport?.({ LitElement: i4 });
var o4 = s3.litElementPolyfillSupport;
o4?.({ LitElement: i4 });
(s3.litElementVersions ??= []).push("4.2.2");

// public/browser/el/browser.js
var import_lit2 = __toESM(require_lib(), 1);
customElements.define("atmos-browser", class AtmosBrowser extends i4 {
  #title = new import_lit2.StoreController(this, $title);
  #url = new import_lit2.StoreController(this, $url);
  #currentTile = new import_lit2.StoreController(this, $currentTile);
  #profile = new import_lit2.StoreController(this, $profile);
  #error = new import_lit2.StoreController(this, $error);
  static properties = {
    mode: { attribute: false }
  };
  static styles = [
    i`
      :host {
        display: block;
      }
      #browser {
        border: 1px solid var(--atmos-mid-fill);
      }
      h2 {
        background-color: var(--atmos-invert-bg);
        text-align: center;
        color: var(--atmos-invert-fg);
        font-weight: 100;
        margin: 0;
        padding: var(--atmos-space-small);
      }
      h2 atmos-dyn-img {
        vertical-align: middle;
      }
      h2 atmos-dyn-img::part(image) {
        border-radius: 4px;
      }
      form {
        background: var(--atmos-shaded);
        border-bottom: 1px solid var(--atmos-mid-fill);
        padding: var(--atmos-space-mid);
        display: flex;
        gap: var(--atmos-space-mid);
      }
      form input[name="url"] {
        flex-grow: 1;
        font-family: inherit;
        font-size: inherit;
        padding: var(--atmos-space-mid);
        border-radius: 4px;
        border: 1px solid var(--atmos-invert-bg);
      }
      form input[name="url"]:focus {
        outline: 2px solid var(--green);
      }
      atmos-tile-card {
        margin: 0 auto;
      }
      .content.card {
        padding: 1rem;
      }
      .context-banner.live {
        display: flex;
      }
      .context-banner.live atmos-profile-tab {
        border-bottom: 1px solid #666;
        border-right: 1px solid #666;
        background: #eee;
      }
      .context-banner.live atmos-tile-tab {
        border-bottom: 1px solid lightgrey;
        flex-grow: 1;
      }
      .nothing {
        min-height: 400px;
        color: #999;
        align-content: center;
        text-align: center;
        font-size: 1.2rem;
      }
    `
  ];
  constructor() {
    super();
    this.mode = "card";
    this.prevURL = this.#url.value;
  }
  // XXX not working
  willUpdate(changedProperties) {
    if (changedProperties.has("tile")) this.mode = "card";
    if (this.prevURL !== this.#url.value) {
      this.prevURL = this.#url.value;
      this.mode = "card";
    }
  }
  handleSubmit(ev) {
    ev.preventDefault();
    const url = ev.currentTarget.querySelector('input[name="url"]')?.value;
    if (!url) return error("URL is empty!");
    if (!/^at:\/\/[^/]+\/ing\.dasl\.masl\/\w+$/.test(url)) {
      return error(`Syntax error, expected "at://<didOrHandle>/ing.dasl.masl/<tid>"`);
    }
    window.location.hash = `#url=${url}`;
  }
  handleClick() {
    this.mode = "live";
  }
  render() {
    let body = A;
    if (this.#error.value) body = b2`<div class="error">${this.#error.value}</div>`;
    else if (this.#currentTile.value) {
      let height = this.#currentTile.value.manifest?.sizing?.height || 300;
      height = Math.min(height, 1e3);
      height = Math.max(height, 300);
      body = b2`<div class=${`context-banner ${this.mode}`}>
          <atmos-profile-tab .profile=${this.#profile.value}></atmos-profile-tab>
          ${this.mode === "live" ? b2`<atmos-tile-tab .tile=${this.#currentTile.value}></atmos-tile-tab>` : A}
        </div>
        <div class=${`content ${this.mode}`}>
          ${this.mode === "live" ? b2`<atmos-tile-live .tile=${this.#currentTile.value} style=${`height: ${height}px`}></atmos-tile-live>` : b2`<atmos-tile-card .tile=${this.#currentTile.value} @click=${this.handleClick}></atmos-tile-card>`}
        </div>
      `;
    } else body = b2`<div class="nothing">ATMOS ready to sail the skies.</div>`;
    return b2`<div id="browser">
      <h2>
        ${this.#currentTile.value?.manifest?.icons?.[0]?.src ? b2`<atmos-dyn-img .tile=${this.#currentTile.value} src=${this.#currentTile.value.manifest.icons[0].src} width="24" height="24" alt="icon"></atmos-dyn-img>` : A}
        ${this.#title.value}
      </h2>
      <form @submit=${this.handleSubmit}>
        <input type="text" name="url" value=${this.#url.value}>
      </form>
      <div id="render-zone">${body}</div>
    </div>`;
  }
});

// public/browser/el/profile-tab.js
var defaultImg = `data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><rect x=%220%22 y=%220%22 width=%22100%22 height=%22100%22 fill=%22%23ff5e5e%22></rect></svg>`;
customElements.define("atmos-profile-tab", class AtmosProfileTab extends i4 {
  static properties = {
    profile: { attribute: false }
  };
  static styles = [
    i`
      :host {
        display: block;
      }
      .banner {
        display: flex;
        gap: 12px;
        padding: 1rem 1rem 12px 1rem;
      }
      .banner img {
        width: 42px;
        height: 42px;
        border-radius: 50%;
      }
      .names {
        display: flex;
        flex-grow: 1;
        flex-direction: column;
      }
      .displayName {
        font-weight: bold;
      }
      .handle {
        font-size: 0.9rem;
      }
    `
  ];
  render() {
    if (!this.profile) return A;
    return b2`<div class="banner">
      <div class="avatar"><img src=${this.profile.avatar || defaultImg} alt="profile picture"></div>
      <div class="names">
        <span class="displayName">${this.profile.displayName || "Anonymous Tiler"}</span>
        <span class="handle">@${this.profile.handle || this.profile.did || "-"}</span>
      </div>
    </div>`;
  }
});

// public/browser/el/tile-card.js
customElements.define("atmos-tile-card", class AtmosTileCard extends i4 {
  static properties = {
    tile: { attribute: false }
  };
  static styles = [
    i`
      :host {
        display: block;
        max-width: 566px;
      }
      #card {
        border: 1px solid lightgrey;
        border-radius: 0.4rem;
        cursor: pointer;
      }
      #card:hover {
        border-color: #666;
      }
      p {
        margin: 0rem 1rem 1rem 1rem;
      }
    `
  ];
  render() {
    if (!this.tile) return A;
    const { manifest } = this.tile;
    if (!manifest) return A;
    return b2`<div id="card">
      ${manifest?.screenshots?.[0]?.src ? b2`<atmos-bg-pic .tile=${this.tile} src=${manifest.screenshots[0].src}></atmos-bg-pic>` : A}
      <atmos-tile-tab .tile=${this.tile}></atmos-tile-tab>
      ${manifest?.description ? b2`<p>${manifest.description}</p>` : A}
    </div>`;
  }
});

// public/browser/el/tile-live.js
customElements.define("atmos-tile-live", class AtmosTileLive extends i4 {
  static properties = {
    tile: { attribute: false },
    height: {}
  };
  static styles = [
    i`
      :host {
        display: block;
      }
      iframe {
        display: block;
        width: 100%;
        height: 100%;
        border: none;
      }
    `
  ];
  firstUpdated() {
    if (!this.tile) {
      console.warn(`Whooooops tile should be available already.`);
      return;
    }
    this.tile.attachIframe(this.renderRoot.querySelector("iframe"));
  }
  render() {
    if (!this.tile) return A;
    console.warn(this.tile);
    return b2`<iframe src=${this.tile.getLoadSource()}></iframe>`;
  }
});

// public/browser/el/tile-tab.js
customElements.define("atmos-tile-tab", class AtmosTileTab extends i4 {
  static properties = {
    tile: { attribute: false }
  };
  static styles = [
    i`
      :host {
        display: block;
      }
      .tab {
        padding: 0.5rem 1rem;
        display: flex;
        align-items: center;
        gap: 12px;
      }
      .tab atmos-dyn-img::part(image) {
        border-radius: 50%;
      }
    `
  ];
  render() {
    if (!this.tile) return A;
    const { manifest } = this.tile;
    if (!manifest) return A;
    return b2`<div class="tab">
      ${manifest?.icons?.[0]?.src ? b2`<atmos-dyn-img .tile=${this.tile} src=${manifest.icons[0].src} width="48" height="48" alt="icon"></atmos-dyn-img>` : A}
      <strong>${manifest.name || "Untitled Tile"}</strong>
    </div>`;
  }
});

// node_modules/@lit/task/task.js
var i5 = Symbol();
var h3 = class {
  get taskComplete() {
    return this.t || (1 === this.i ? this.t = new Promise((t3, s5) => {
      this.o = t3, this.h = s5;
    }) : 3 === this.i ? this.t = Promise.reject(this.l) : this.t = Promise.resolve(this.u)), this.t;
  }
  constructor(t3, s5, i6) {
    this.p = 0, this.i = 0, (this._ = t3).addController(this);
    const h4 = "object" == typeof s5 ? s5 : { task: s5, args: i6 };
    this.v = h4.task, this.j = h4.args, this.m = h4.argsEqual ?? r4, this.k = h4.onComplete, this.A = h4.onError, this.autoRun = h4.autoRun ?? true, "initialValue" in h4 && (this.u = h4.initialValue, this.i = 2, this.O = this.T?.());
  }
  hostUpdate() {
    true === this.autoRun && this.S();
  }
  hostUpdated() {
    "afterUpdate" === this.autoRun && this.S();
  }
  T() {
    if (void 0 === this.j) return;
    const t3 = this.j();
    if (!Array.isArray(t3)) throw Error("The args function must return an array");
    return t3;
  }
  async S() {
    const t3 = this.T(), s5 = this.O;
    this.O = t3, t3 === s5 || void 0 === t3 || void 0 !== s5 && this.m(s5, t3) || await this.run(t3);
  }
  async run(t3) {
    let s5, h4;
    t3 ??= this.T(), this.O = t3, 1 === this.i ? this.q?.abort() : (this.t = void 0, this.o = void 0, this.h = void 0), this.i = 1, "afterUpdate" === this.autoRun ? queueMicrotask(() => this._.requestUpdate()) : this._.requestUpdate();
    const r5 = ++this.p;
    this.q = new AbortController();
    let e4 = false;
    try {
      s5 = await this.v(t3, { signal: this.q.signal });
    } catch (t4) {
      e4 = true, h4 = t4;
    }
    if (this.p === r5) {
      if (s5 === i5) this.i = 0;
      else {
        if (false === e4) {
          try {
            this.k?.(s5);
          } catch {
          }
          this.i = 2, this.o?.(s5);
        } else {
          try {
            this.A?.(h4);
          } catch {
          }
          this.i = 3, this.h?.(h4);
        }
        this.u = s5, this.l = h4;
      }
      this._.requestUpdate();
    }
  }
  abort(t3) {
    1 === this.i && this.q?.abort(t3);
  }
  get value() {
    return this.u;
  }
  get error() {
    return this.l;
  }
  get status() {
    return this.i;
  }
  render(t3) {
    switch (this.i) {
      case 0:
        return t3.initial?.();
      case 1:
        return t3.pending?.();
      case 2:
        return t3.complete?.(this.value);
      case 3:
        return t3.error?.(this.error);
      default:
        throw Error("Unexpected status: " + this.i);
    }
  }
};
var r4 = (s5, i6) => s5 === i6 || s5.length === i6.length && s5.every((s6, h4) => !f(s6, i6[h4]));

// public/browser/el/bg-pic.js
customElements.define("atmos-bg-pic", class AtmosBackgroundPicture extends i4 {
  #bgTask = new h3(this, {
    task: async ([tile, src]) => {
      const res = await tile.resolvePath(src);
      if (!res.ok) {
        throw new Error(res.status);
      }
      const blob = new Blob([res.body], { type: res.headers?.["content-type"] });
      return URL.createObjectURL(blob);
    },
    args: () => [this.tile, this.src]
  });
  static properties = {
    tile: { attribute: false },
    src: {}
  };
  static styles = [
    i`
      :host {
        display: block;
        aspect-ratio: 16/9;
      }
      div {
        background-image: var(--bg-src);
        background-size: cover;
        background-position: 50%;
        height: 100%;
      }
    `
  ];
  render() {
    if (!this.tile || !this.src) return A;
    return this.#bgTask.render({
      pending: () => b2`<div>Loading…</div>`,
      complete: (url) => b2`<div style=${`--bg-src: url(${url})`}></div>`,
      error: (e4) => {
        console.warn(e4);
        return A;
      }
    });
  }
});

// public/browser/el/dyn-img.js
customElements.define("atmos-dyn-img", class AtmosDynamicImage extends i4 {
  #bgTask = new h3(this, {
    task: async ([tile, src]) => {
      const res = await tile.resolvePath(src);
      if (!res.ok) {
        throw new Error(res.status);
      }
      const blob = new Blob([res.body], { type: res.headers?.["content-type"] });
      return URL.createObjectURL(blob);
    },
    args: () => [this.tile, this.src]
  });
  static properties = {
    tile: { attribute: false },
    src: {},
    width: {},
    height: {},
    alt: {}
  };
  static styles = [
    i`
      :host {
        display: inline-block;
      }
    `
  ];
  render() {
    if (!this.tile || !this.src) return A;
    return this.#bgTask.render({
      pending: () => b2`<div>…</div>`,
      complete: (url) => b2`<img src=${url} width=${this.width} height=${this.height} alt=${this.alt} part="image">`,
      error: (e4) => {
        console.warn(e4);
        return A;
      }
    });
  }
});

// public/browser/at.js
window.addEventListener("hashchange", navigateFromHash);
window.addEventListener("load", navigateFromHash);
/*! Bundled license information:

@lit/reactive-element/css-tag.js:
  (**
   * @license
   * Copyright 2019 Google LLC
   * SPDX-License-Identifier: BSD-3-Clause
   *)

@lit/reactive-element/reactive-element.js:
  (**
   * @license
   * Copyright 2017 Google LLC
   * SPDX-License-Identifier: BSD-3-Clause
   *)

lit-html/lit-html.js:
  (**
   * @license
   * Copyright 2017 Google LLC
   * SPDX-License-Identifier: BSD-3-Clause
   *)

lit-element/lit-element.js:
  (**
   * @license
   * Copyright 2017 Google LLC
   * SPDX-License-Identifier: BSD-3-Clause
   *)

lit-html/is-server.js:
  (**
   * @license
   * Copyright 2022 Google LLC
   * SPDX-License-Identifier: BSD-3-Clause
   *)

@lit/task/task.js:
  (**
   * @license
   * Copyright 2017 Google LLC
   * SPDX-License-Identifier: BSD-3-Clause
   *)
*/
//# sourceMappingURL=at.min.js.map
